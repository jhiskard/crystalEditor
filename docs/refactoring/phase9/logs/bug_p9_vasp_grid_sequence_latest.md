# P9-BUG-01 재현 로그 (초기 등록)

등록일: `2026-04-02 (KST)`  
이슈 ID: `P9-BUG-01`

## 1. 증상
- `XSF(Grid) -> VASP -> XSF(Grid) -> VASP` 순서로 파일을 로드하면,
  이전 grid 데이터가 완전히 제거되지 않고 잔존하는 현상이 관찰된다.

## 2. 현재 판정
- 상태: `Open`
- 우선순위: `Medium`
- 비고: Phase 8 종료 시점에서 수정 시도 2건 롤백 완료, Phase 9에서 추적 지속

## 3. Phase 9 처리 계획
1. W0에서 재현 절차를 자동/수동 모두 고정
2. W3~W4에서 render ownership 정리 경로 점검
3. W6 종료 문서에 해결/이관(Phase 10) 여부를 명시
