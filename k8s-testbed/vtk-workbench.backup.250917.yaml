apiVersion: apps/v1
kind: Deployment
metadata:
  name: vtk-workbench
  namespace: pt
  labels:
    app: vtk-workbench
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vtk-workbench
  template:
    metadata:
      labels:
        app: vtk-workbench
        version: v1
    spec:
      containers:
      - name: vtk-workbench
        image: 10.183.3.3:5000/vtk-workbench:1.7
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 3000
          protocol: TCP
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3000"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
# 헬스체크 제거 - 간단한 배포를 위해
        # livenessProbe:
        #   httpGet:
        #     path: /crystal-editor/api/health
        #     port: http
        #   initialDelaySeconds: 30
        #   periodSeconds: 10
        # readinessProbe:
        #   httpGet:
        #     path: /crystal-editor/api/health
        #     port: http
        #   initialDelaySeconds: 5
        #   periodSeconds: 5
        securityContext:
          runAsNonRoot: true
          runAsUser: 1001
          runAsGroup: 1001
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL
      securityContext:
        fsGroup: 1001
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: vtk-workbench-service
  namespace: pt
  labels:
    app: vtk-workbench
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: vtk-workbench
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vtk-workbench-ingress
  namespace: pt
  labels:
    app: vtk-workbench
  annotations:
    # Enable SSL redirect
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"

    # Increase proxy timeout for WebAssembly loading
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"

    # Enable large file uploads if needed
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"

    # Enable CORS
    nginx.ingress.kubernetes.io/enable-cors: "true"

    # Enable regex support for path matching
    nginx.ingress.kubernetes.io/use-regex: "true"

    # Custom rewriting logic for different paths
    nginx.ingress.kubernetes.io/configuration-snippet: |
      # Security headers
      add_header X-Frame-Options "DENY" always;

      # Conditional rewriting based on path
      if ($request_uri ~ ^/crystal-editor/(.*)$) {
        rewrite ^/crystal-editor/(.*)$ /$1 break;
      }

spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - portal-test.edison.re.kr
    secretName: vtk-workbench-tls
  rules:
  - host: portal-test.edison.re.kr
    http:
      paths:
      # Handle /crystal-editor path with rewriting
      - path: /crystal-editor/(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: vtk-workbench-service
            port:
              number: 80
      # Handle direct asset requests without rewriting
      - path: /(_next|wasm)/(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: vtk-workbench-service
            port:
              number: 80